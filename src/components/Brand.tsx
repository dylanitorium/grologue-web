import { css } from "@emotion/css";

const Brand = () => (
  <h1
    className={css`
      font-family: "Poppins", serif;
      color: #000000;
      font-size: 35px;
    `}
  >
    Grologue
  </h1>
);

export default Brand;
